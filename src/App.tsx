import ListGroup from "./components/ListGroup";

function App() {
  // Self closing syntax
  return (
    <div>
      <ListGroup />
      <ListGroup />
    </div>
  );
}

// Export so it can be used somewhere else
export default App;
